server:
  port: 7071

spring:
  application:
    name: productStorage
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true

management:
  endpoint:
    health:
      show-details: always # never or when-authorized
  endpoints:
    web:
      exposure:
        include: metrics,prometheus