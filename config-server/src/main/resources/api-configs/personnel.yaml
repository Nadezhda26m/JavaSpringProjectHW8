# Настройки приложения personnel-service
server:
  port: 8082

spring:
  profiles:
    include:
      - eclient
    # from application-eclient.yaml and personnel-eclient.yaml if exists

  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
